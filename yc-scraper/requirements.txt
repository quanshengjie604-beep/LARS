# YC Scraper Dependencies
# Compatible with Python 3.11, 3.12, and 3.13

# Recommended: Algolia-based scraper (algolia_extractor.py + enrich_companies.py)
# uses only the Python standard library. certifi is optional but recommended so
# TLS verification works on Python builds that don't trust the system CA store.
certifi>=2024.2.2

# Core scraping dependencies (only needed for the legacy Selenium/Scrapy path)
scrapy==2.11.2
selenium==4.15.2
tqdm==4.66.4

# Data analysis (optional - use pandas>=2.2.0 for Python 3.13 support)
pandas>=2.2.0

# Additional helpful packages
lxml==5.1.0  # Required by Scrapy for parsing